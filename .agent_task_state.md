🚩 目標：修復 Phaser 遊戲中的 Maximum call stack size exceeded 錯誤與安全性警告。
✅ 進展：
1. 分析代碼發現 `killEnemy` 與 `checkChainKill` 之間存在無限遞迴邏輯。
2. 已修復 `index-standalone.html` 中的遞迴錯誤。
3. 已修復 `js/scenes/GameScene.js` 中的遞迴錯誤。
4. 已建立 `CHANGELOG.md` 並記錄變更。
🚀 下一步：建議使用者使用本機 Web Server 執行遊戲以解決 `file://` 安全性警告，並進行 Git 提交。
